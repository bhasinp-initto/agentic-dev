# macOS
.DS_Store

# Editors
.vscode/
.idea/
*.swp
*~

# Local env files — must NEVER be committed
.env
.env.*
*.env
!schemas/*.env.schema.yaml

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# Node
node_modules/

# Ruflo / RuVector local DB
ruvector.db
agentdb.rvf
agentdb.rvf.lock
*.rvf
*.rvf.lock

# Plugin-local test artifacts
tests/**/.tmp/
tests/**/tmp/

# Per-host-project worktrees created by the agentic-dev system at runtime
.worktrees/

# Claude Code session artifacts that should stay local
.claude/sessions/
